[[search-aggregations-matrix]]
== 矩阵聚合（Matrix Aggregations）

experimental[]

这组聚合模块会在操作多个字段然后生成一个矩阵，这个生成过程使用的基础数据是从请求文档的字段中提取的。与指标和分桶聚合不同，这个组聚合模块暂时还不支持脚本。

include::matrix/stats-aggregation.asciidoc[]